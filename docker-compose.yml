# Kodo Knowledge Agent — full stack (PRD §21).
# Named volumes persist data across restarts (avoid data loss + costly re-embed).

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: kodo
      POSTGRES_PASSWORD: kodo
      POSTGRES_DB: kodo_knowledge
    ports:
      # Host 5544 -> Postgres 5432 (connect a GUI like TablePlus/DBeaver here).
      - "5544:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U kodo -d kodo_knowledge"]
      interval: 10s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  qdrant:
    image: qdrant/qdrant:v1.12.4
    volumes:
      - qdrantdata:/qdrant/storage
    ports:
      # Host 6399 -> Qdrant 6333. Browser dashboard: http://localhost:6399/dashboard
      - "6399:6333"
    # The qdrant image ships no shell/curl, so we don't declare a healthcheck;
    # app services depend on service_started and retry against Qdrant at runtime.
    restart: unless-stopped

  rabbitmq:
    image: rabbitmq:3.13-management
    volumes:
      - rabbitmqdata:/var/lib/rabbitmq
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 10s
      timeout: 10s
      retries: 10
    restart: unless-stopped

  # One-shot migration step; app services wait for it to complete successfully.
  migrate:
    build: .
    command: ["alembic", "upgrade", "head"]
    env_file: .env
    depends_on:
      postgres:
        condition: service_healthy
    restart: "no"

  api:
    build: .
    command: ["python", "-m", "app.api"]
    env_file: .env
    ports:
      # Host 8899 -> container 8000 (host 8000 is used by another local app).
      - "8899:8000"
    depends_on:
      migrate:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
      qdrant:
        condition: service_started
      rabbitmq:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8000/health"]
      interval: 15s
      timeout: 5s
      retries: 5
    mem_limit: 1g
    restart: unless-stopped

  worker:
    build: .
    command:
      - "celery"
      - "-A"
      - "app.workers.celery_app:celery_app"
      - "worker"
      - "-Q"
      - "knowledge"
      - "--loglevel=INFO"
      - "--concurrency=4"
      - "--prefetch-multiplier=1"
    env_file: .env
    depends_on:
      migrate:
        condition: service_completed_successfully
      rabbitmq:
        condition: service_healthy
      postgres:
        condition: service_healthy
      qdrant:
        condition: service_started
    mem_limit: 2g
    restart: unless-stopped

  beat:
    build: .
    command:
      - "celery"
      - "-A"
      - "app.workers.celery_app:celery_app"
      - "beat"
      - "--loglevel=INFO"
    env_file: .env
    depends_on:
      migrate:
        condition: service_completed_successfully
      rabbitmq:
        condition: service_healthy
    restart: unless-stopped

  # Slack @mention bot over Socket Mode (no public URL). Needs SLACK_APP_TOKEN.
  slackbot:
    build: .
    command: ["python", "-m", "app.slackbot"]
    env_file: .env
    depends_on:
      rabbitmq:
        condition: service_healthy
    restart: unless-stopped

volumes:
  pgdata:
  qdrantdata:
  rabbitmqdata:
