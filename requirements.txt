# Kodo Knowledge Agent — pinned dependencies (no uv; plain venv + pip)
# Install: python -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

# --- web / api ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# --- database ---
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.14.0

# --- background jobs ---
celery==5.4.0

# --- vector store ---
qdrant-client==1.12.1

# --- llm / embeddings ---
openai==1.59.0
tiktoken==0.8.0

# --- slack ---
slack_sdk==3.33.4

# --- http + file extraction ---
httpx==0.28.1
pypdf==5.1.0
python-docx==1.1.2

# --- resilience ---
tenacity==9.0.0
